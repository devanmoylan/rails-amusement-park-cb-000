<%= form_for :user, url: sign_in_path do |f| %>
    <p>Username: <%= f.collection_select :id, User.all, :id, :name %></p>
    <p>Password: <%= f.password_field :password %></p>
    <%= f.submit %>
<% end %>
